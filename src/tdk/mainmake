CXX=g++
INC=-I/home/tk/develop/tdk/src
CPPFLAGS=-std=c++11 -g $(INC)
OBJECTS=main.o
OBJS_DIR=obj

TARGET=main

#.SUFFIXES: .cpp .o

all: $(TARGET)

$(TARGET): $(OBJECTS)
	$(CC) -o  $@ $(OBJECTS) -L. -ltdk -lstdc++

clean:
	rm -rf $(OBJECTS)
