#ifndef __tdk_io_net_basic_socket_h__
#define __tdk_io_net_basic_socket_h__

#include <tdk/network/address.hpp>

namespace tdk {
namespace io {
namespace ip {
	/*
class basic_socket {
public:
	basic_socket( void );
	basic_socket( const basic_socket& rhs );
	basic_socket( SOCKET fd );

	basic_socket& operator=( const basic_socket& rhs );

	bool open( const int af , const int type , const int proto );
	void 


protected:
	~basic_socket( void );
private:
	SOCKET _handle;
};

*/
}}}


#endif